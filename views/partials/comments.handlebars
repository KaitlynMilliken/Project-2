<div class="row justify-content-center">
  <div class="post col-8">
    {{#each this}}
    <section class="comment">
      <div class="meta">
        {{user.username}}
      </div>
      <div class="text">
        {{comment_text}}
      </div>
    </section>
    {{/each}}
  </div>
</div>