<div class="row justify-content-center">
    <div class="post col-8 mt-3 text-center mt-2 shadow-lg p-3 mb-5 rounded p-5">
        <span class="display-4">{{ post.title }}</span>
        <p class="mt-3">{{ post.description }}</p>
        {{> comments post.comments }}
    </div>
</div>